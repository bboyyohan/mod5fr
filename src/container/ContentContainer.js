import React from 'react'
// import LogContainer from './LogContainer'

class ContentContainer extends React.Component {
    // state = {
    //     logs: []
    // }

    

    componentDidMount() {
        // this.fetchLogs()
    }

    //maybe add fetch sleep + meals later here?
    
    render(){
        return(
            <div>

                {/* <LogContainer currentUser={this.props.currentUser} logs={this.props.logs}/> */}
                
            </div>
        )
    }
}

export default ContentContainer